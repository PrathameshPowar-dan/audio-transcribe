export default function Home() {
    return (
        <main style={{ padding: "2rem", fontFamily: "sans-serif" }}>
            <h1>Audio Transcriber Frontend</h1>
            <p>The backend APIs are ready. Time to build the UI!</p>
        </main>
    );
}